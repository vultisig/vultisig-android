Approve PR $ARGUMENTS and add it to the merge queue.

1. Run: `gh pr review $ARGUMENTS --repo vultisig/vultisig-android --approve --body "LGTM"`
2. Run: `gh pr merge $ARGUMENTS --repo vultisig/vultisig-android --auto`
3. Confirm both succeeded.